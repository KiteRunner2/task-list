{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle index.ts --outfile=./build/index.js --platform=node --target=node16.13.1 --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "mongodb": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.3",
    "esbuild": "^0.14.48",
    "typescript": "^4.7.4"
  }
}
